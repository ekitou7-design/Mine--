# Mine - Minimalist Digital Planner

A serene, tactile digital planner that runs locally in your browser and stores data in localStorage.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Open the local URL shown in your terminal.
